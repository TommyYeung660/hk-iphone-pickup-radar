# 香港 iPhone 18 Pro Max 1TB 库存监控

监控目标：

- 冰川色：`MJY14ZA/A`
- 布根地红色：`MJY04ZA/A`
- 地区：香港全部 6 家 Apple Store

## 第一次启动

1. 右键 `start-monitor.ps1`，选择“使用 PowerShell 运行”。
2. 第一次会安装运行组件，完成后终端会显示电脑和手机访问地址。
3. 手机与电脑连接同一 Wi-Fi，在手机浏览器打开终端显示的 `http://...:3000` 地址。
4. 点“开始”，再点“开启有货通知”。建议把网页添加到手机主屏幕。

Windows 如果询问是否允许网络访问，只需允许“专用网络”。库存查询服务本身只监听电脑内部地址，手机访问的是监控面板。

## 下单前准备

1. 在准备下单的手机浏览器打开 Apple 香港官网并登录 Apple ID。
2. 把目标 iPhone 配置加入购物袋，并提前准备好收货人和付款信息。
3. 出现有货提醒后，在监控页点“打开 Apple 结账”。Apple 会再次校验门店库存。
4. 检查型号、颜色、容量、门店和金额，由你本人完成最终付款。

监控程序不会读取或保存 Apple ID、密码、验证码、银行卡或 CVV。它也不会自动提交订单。

## 使用限制

- 电脑必须保持开机，`start-monitor.ps1` 的窗口也要保持运行。
- 电脑端每 60 秒向 Apple 查询一次；页面可选择 30 秒、1 分钟或 2 分钟刷新显示。
- 普通手机网页的声音、震动和系统通知通常要求页面保持打开。若以后需要锁屏也能收到提醒，可再接入 Bark 等推送服务。
- Apple 的库存接口并非公开 API，Apple 可能随时调整；结账页显示的结果始终优先。

## 停止

回到 PowerShell 窗口按 `Ctrl+C`。如电脑重启，重新运行 `start-monitor.ps1` 即可。
